name: Verify & Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  JAVA_HOME: /usr/lib/jvm/temurin-17-jdk-amd64
  ANDROID_HOME: ${{ github.workspace }}/android-sdk
  ANDROID_SDK_ROOT: ${{ github.workspace }}/android-sdk
  ANDROID_NDK_HOME: ${{ github.workspace }}/android-sdk/ndk/26.1.10909125
  PATH: ${{ github.workspace }}/android-sdk/cmdline-tools/latest/bin:${{ github.workspace }}/android-sdk/platform-tools:${{ env.PATH }}

jobs:
  # ================== 渲染验证（Material Design 回归检测）==================
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install jsdom
        run: npm install --no-audit --no-fund jsdom
      - name: 运行渲染验证（各页面 + 端到端交互）
        run: node _md_verify.js

  # ================== 构建安卓 APK ==================
  build-android:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: read   # 仅读取仓库
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 手动安装 Android SDK command-line tools（避开第三方 action 的权限坑）
      - name: Setup Android SDK
        run: |
          set -e
          SDK=${{ env.ANDROID_HOME }}
          mkdir -p "$SDK/cmdline-tools"
          echo "下载 command-line tools ..."
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O /tmp/cmdtools.zip
          unzip -q /tmp/cmdtools.zip -d "$SDK/cmdline-tools"
          mv "$SDK/cmdline-tools/cmdline-tools" "$SDK/cmdline-tools/latest"
          # 接受所有授权
          yes | sdkmanager --sdk_root="$SDK" --licenses >/dev/null 2>&1 || true
          # 安装构建所需组件
          sdkmanager --sdk_root="$SDK" \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0" \
            "ndk;26.1.10909125" \
            "cmdline-tools;latest"
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "PATH=$SDK/cmdline-tools/latest/bin:$SDK/platform-tools:$PATH" >> $GITHUB_ENV
          adb --version || true

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Install deps & sync Capacitor
        run: |
          set -e
          npm install
          # 若 android 平台未生成则创建
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          npx cap sync android

      - name: Build APK (debug)
        working-directory: android
        run: |
          set -e
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: agentforge-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: android/app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ================== 部署 GitHub Pages（PWA 网页版）==================
  # Pages 使用仓库 main 分支根目录（legacy 模式），push 即自动部署，
  # 无需额外的 deploy-pages job（避免 id-token/pages 写权限要求）。
  deploy-pages:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: 提示 Pages 部署方式
        run: |
          echo "Pages 已配置为 legacy 模式（source: main /），"
          echo "push 到 main 后 GitHub 会自动构建部署，无需本 job 额外操作。"
          echo "若需强制重建，可在仓库 Settings → Pages 点击 Rebuild。"
