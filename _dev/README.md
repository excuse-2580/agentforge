# 开发工具 / Dev Tools

本目录存放开发期辅助脚本，不属于应用运行所需文件。

- `_md_verify.js` — 用 jsdom 做无头 DOM 渲染验证，确保各页面渲染无 JS 错误（CI 可接入）
- `_design_preview.py` / `_compose_preview.py` — 生成 Material Design 3 设计预览图
- 其余 `*.py` / `build-apk.sh` — 早期 APK 构建探索脚本（保留供参考，非必需）
